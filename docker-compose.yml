version: "3.8"

services:
  server:
    build:
      context: ./server
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - HOST=0.0.0.0

  client:
    build:
      context: ./client
    ports:
      - "8080:80"
    depends_on:
      - server
